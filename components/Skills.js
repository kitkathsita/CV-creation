export default function Skills({ group, skills }) {
    return (
        <div className="groupin">
            <div className="group">{group}</div>
            <div className="skill">{skills}</div>
        </div>
    )
}